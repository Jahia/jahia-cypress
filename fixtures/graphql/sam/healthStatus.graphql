query($severity: GqlProbeSeverity) {
    admin {
        jahia {
            healthCheck(severity: $severity) {
                status {
                    health
                    message
                }
                probes(health: YELLOW) {
                    name
                    status {
                        health
                        message
                    }
                }                
            }
        }
    }
}